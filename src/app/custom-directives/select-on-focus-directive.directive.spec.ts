import { SelectOnFocusDirectiveDirective } from './select-on-focus.directive';

describe('SelectOnFocusDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectOnFocusDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
